{% extends "base.html.twig" %}

{% block body %}

    <div class="row" style="margin-left: 50px; margin-top: 20px; margin-right: 20px;">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <div>Current amount of records in the database: ({{ counter.getTotalItemCount }})</div>
                    <a href="{{path('upload_emails')}}">
                        <button class="btn btn-primary">Upload emails</button>
                    </a>
                </div>
                <div style="height: 150px;">
                    <a href="{{path('send_emails')}}"><button class="btn btn-primary" style="margin-top: 30px; margin-left: 30px;">Send emails</button></a>
                    <div style="margin-top: 30px; margin-left: 30px;">All time total records uploaded: 0</div>
                </div>

            </div>
        </div>
    </div>
    </div>
    <div>
{% endblock %}