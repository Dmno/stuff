{% extends "base.html.twig" %}

{% block body %}

    <div class="row" style="margin-left: 50px; margin-top: 20px; margin-right: 20px;">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <div>Total results: ({{ pagination.getTotalItemCount }})</div>
                    <a href="{{path('new_author')}}">
                        <button  class="btn btn-primary">Add new author</button>
                    </a>

                    <form>
                        <div class="input-group mb-3">
                            <input type="text"
                                   name="a"
                                   class="form-control"
                                   placeholder="Search an author"
                            >
                            <div class="input-group-append">
                                <button type="submit"
                                        class="btn btn-outline-secondary">
                                    <span class="fa fa-search"></span>
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
                <table class="table">
                    <thead>
                    <tr>
                        <th>{{ knp_pagination_sortable(pagination, 'ID', 'h.id') }}<div class="sort fa fa-sort"></div></th>
                        <th>{{ knp_pagination_sortable(pagination, 'Name', 'h.fullName') }}<div class="sort fa fa-sort"></div></th>
                        <th style="text-align: center;">Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for author in pagination %}
                        <tr>
                            <th>{{ author.id }}</th>
                            <td>{{ author.fullName }}</td>
                            <td align="center">
                                <a href="{{ path('edit_author', {'id': author.id})}}">
                                    <button class="btn btn-primary">Edit</button>
                                </a>
                                <a href="{{ path('delete_author', {'id': author.id})}}">
                                    <button class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this author?')">Delete</button>
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
                {{ knp_pagination_render(pagination) }}
            </div>
        </div>
    </div>
    </div>
    <div>
{% endblock %}