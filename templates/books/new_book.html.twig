{% extends 'base.html.twig' %}

{% block body %}
    <div style="margin-left: 50px; margin-top: 50px; margin-right: 50px; padding: 30px; background: white;">
    {{ title }}
    {{ form(form) }}
    </div>
{% endblock %}